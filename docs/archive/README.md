# Archive

Dated historical material. Naming: `YYYY-MM-DD-<slug>.md`.

- Append-only: files here are never edited after filing.
- Not current: **agents must not read this folder for current project state.**
  Read it only when the task is explicitly about history or provenance.
- Current state lives in `docs/STATUS.md`; durable reasoning in
  `docs/DECISIONS.md`.
